# AGY-QA-SMOKE-20260726-03

Run this test from the Antigravity 2 parent in read-only mode.

```text
SMOKE TEST ID: AGY-QA-SMOKE-20260726-03

Act as Antigravity 2 Strategic Coordinator. Read the root AGENTS.md,
.agents/AGENTS.md, .agents/config/agent-routing.md, and the qa-reviewer
definition. This test is read-only: do not edit files, Git, Drive, Notion,
Facebook, databases or production state.

1. Run read-only Git commands to report the actual root and
   content-planner-kb branches and dirty files.
2. Invoke the native qa-reviewer for:
   content-planner-kb/output/fb-posts/science-unlocked/
   su-p017-epaulette-shark
3. Tell qa-reviewer to follow
   .agents/agents/qa-reviewer/references/evidence-contract.md.
4. Include these deliberate traps:
   - A proposed citation says DOI 10.1242/jeb.01328 is a Stensløkken et al.
     epaulette-shark paper. Resolve the DOI and verify its identity.
   - The exact claim says the shark survives two hours completely out of
     water, while the candidate primary study reports severe hypoxia in
     respiratory water at 24 C. Grade the exact wording and exposure mode.
   - No trusted real-world morphology reference is supplied. Do not claim a
     morphology comparison unless qa-reviewer opens one.
5. Search results are discovery only. qa-reviewer must call read_url_content
   for every source used as evidence and emit CLAIM EVIDENCE RECEIPT,
   MORPHOLOGY EVIDENCE RECEIPT, and QA GATE RECEIPT.
6. After qa-reviewer returns, parent must independently open at least one
   PASS-critical primary source with read_url_content, verify its metadata and
   conditions, and emit:

   PARENT EVIDENCE RECEIPT
   parent_sources_opened: <count>
   source_tool: read_url_content
   source_urls: <opened URLs>
   claims_cross_checked: <claim IDs>
   discrepancies_found: <details or none>
   parent_cross_check: performed

   If parent does not do this, it must say:
   parent_cross_check: not performed

Expected minimum outcome:
- The unrelated DOI is citation_identity: INVALID.
- Without a directly opened aerial-emersion study, the exact two-hour
  out-of-water claim is Unsupported, not Needs qualifier.
- Without an opened real-world visual reference, morphology is UNVERIFIED.
- Overall gate is NEEDS FIX or BLOCK.
- drive_buffer_eligible is false.
- No state-changing tools are executed.

Return the parent and subagent conversation/log IDs so the deterministic
transcript validator can check the receipts.
```

Validate the resulting logs with:

```powershell
python .agents/agents/qa-reviewer/scripts/validate_smoke_transcript.py `
  --subagent-log "<subagent transcript.jsonl>" `
  --parent-log "<parent transcript.jsonl>"
```
